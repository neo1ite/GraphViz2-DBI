2.54 2026-03-31
- create(): FK edges to id column use headport [port0,w] not "port0:w" (fixes GraphViz2 encoding : as %3a and dot warnings)
- default GraphViz2 theme: light background, Mrecord-filled nodes, splines, spacing; scripts/dbi.schema.pl matches
- GRAPHVIZ2_DBI_DEBUG: trace phases (TableInfo per schema, nodes, FK edges, timings); scripts/dbi.schema.pl traces connect, create, Graphviz run; prepend use lib for local GraphViz2::DBI
- PostgreSQL: merge DBIx::Admin::TableInfo per schema when DBI_SCHEMA is set (TableInfo alone only queries public, ignoring search_path); qualify FK endpoints as schema.table
- fix scripts/dbi.schema.pl: DBI_SCHEMA was broken (Perl && returned 1, not schema list), breaking SET search_path
- add GRAPHVIZ2_DBI_DEBUG and create(debug => 1) diagnostics for empty/small graphs

2.53 2026-03-30
- add create_inheritance() for PostgreSQL INHERITS (pg_inherits) and inheritance_graph accessor
- add t/inheritance.t (mock DBI for Pg)
- document new API in POD

2.52 2020-11-05
- update tests for GraphViz2 2.60

2.51 2020-10-31
- use combine_node_and_port => 0 from GraphViz2 2.58, new record API
- stop using special cases and heuristics for FKs, just use DBI
- zap "name" arg to create, databases don't have a "root"

2.50 2020-10-11
- fix bad logger default

2.49 2020-09-27
- separated out of GraphViz2 2.48
